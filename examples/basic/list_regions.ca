type List a =
    | Node (List a, a)
    | Empty

let head = fun list -> case list of
    | Empty -> None
    | Node (_, e) -> Some e
end
region @test
let alloc = fun 'a () ->
    let 'a a = Empty in a
let @test (t1: List Int) = Node (Empty, 2)
in head t1